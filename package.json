{
  "name": "backdoor-flush",
  "version": "2.5.0",
  "description": "A small poker hand review and study desk.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "FELTSCOPE_PORT=3400 node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "keywords": [
    "poker",
    "analytics",
    "hand-history"
  ],
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.738.0",
    "@aws-sdk/client-s3": "^3.738.0",
    "@aws-sdk/client-sqs": "^3.738.0",
    "@aws-sdk/lib-dynamodb": "^3.738.0"
  },
  "engines": {
    "node": ">=18"
  }
}
